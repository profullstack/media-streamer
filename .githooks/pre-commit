#!/bin/sh
# BitTorrented.com Pre-commit Hook
# Runs formatter on staged files, security audit, and GitHub Actions locally via act

echo "🎨 Running formatter on staged files..."

# Get list of staged JS/TS files
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACMR | grep -E '\.(js|jsx|ts|tsx)$')

if [ -n "$STAGED_FILES" ]; then
    # Run ESLint with --fix on staged files
    echo "$STAGED_FILES" | xargs nice pnpm eslint --fix 2>/dev/null

    # Re-add the formatted files to staging
    echo "$STAGED_FILES" | xargs git add

    echo "✅ Formatter applied to staged files"
else
    echo "ℹ️  No JS/TS files to format"
fi

echo ""
echo "🔐 Running security audit..."
nice pnpm audit --audit-level=critical
AUDIT_EXIT_CODE=$?

if [ $AUDIT_EXIT_CODE -ne 0 ]; then
    echo ""
    echo "❌ CRITICAL security vulnerabilities found! Please fix before committing."
    exit 1
fi

echo "✅ No critical security vulnerabilities found"
echo ""
echo "🔍 Running TypeScript type check..."
nice pnpm typecheck
TYPECHECK_EXIT_CODE=$?

if [ $TYPECHECK_EXIT_CODE -ne 0 ]; then
    echo ""
    echo "❌ TypeScript type errors found! Please fix before committing."
    exit 1
fi

echo "✅ TypeScript type check passed"
echo ""
echo "🧪 Running tests..."
nice pnpm test
TEST_EXIT_CODE=$?

if [ $TEST_EXIT_CODE -ne 0 ]; then
    echo ""
    echo "❌ Tests failed! Please fix before committing."
    exit 1
fi

echo "✅ All tests passed"
echo ""
echo "🚀 Pre-commit checks complete. Proceeding with commit..."

#!/bin/sh
# BitTorrented.com Pre-commit Hook
# Runs GitHub Actions locally via act before every commit

echo "🔨 Running pre-commit checks via act..."
echo ""

act
ACT_EXIT_CODE=$?

if [ $ACT_EXIT_CODE -ne 0 ]; then
    echo ""
    echo "❌ act failed! Please fix issues before committing."
    exit 1
fi

echo ""
echo "🚀 Pre-commit checks complete. Proceeding with commit..."
